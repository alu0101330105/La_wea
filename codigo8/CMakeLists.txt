cmake_minimum_required(VERSION 2.8)
project( ConvexityDefects )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( conv_defects  main.cpp )
target_link_libraries( conv_defects ${OpenCV_LIBS} )
